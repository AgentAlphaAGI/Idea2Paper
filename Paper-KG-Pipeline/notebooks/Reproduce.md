# Notes for repreduce the notebooks

## set up venv

```bash
python3 -m venv .venv
source .venv/bin/activate     # mac / linux
python -m pip install -U pip
```

## Install library

```bash
pip install torch torchvision torchaudio

pip install -r requirements.txt
```

## Reproduce extraction, embedding and clustering

notebooks/01_problem_solution_clustering.ipynb
